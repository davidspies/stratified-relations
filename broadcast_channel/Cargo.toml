[package]
name = "broadcast_channel"
version = "0.1.0"
edition = "2024"
license-file = "../LICENSE.txt"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
derive-where.workspace = true

swap_channel.workspace = true
